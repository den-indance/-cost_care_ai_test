⚖️ Принятые упрощения (Trade-offs)

Для ускорения разработки и упрощения архитектуры были допущены следующие ограничения:
Архитектура и дизайн

    Упрощенный DDD: Мы не используем полноценную реализацию Domain-Driven Design, чтобы избежать избыточного кода.

    Отсутствие DI: Внедрение зависимостей (Dependency Injection) для компонентов и сервисов выполняется вручную, без использования специальных контейнеров.

    Базовое тестирование: Код покрыт только основными юнит-тестами.

    Исключения: Используются только общие (стандартные) исключения вместо иерархии собственных типов ошибок.

    Логирование: Конфигурация логгера жестко задана в коде и не вынесена в отдельный файл.


Интеграция с Google Calendar

    Тестовый режим (Test Mode): Работа ведется с жестко заданными учетными данными одного аккаунта. В текущей версии отсутствуют:

        Поддержка нескольких аккаунтов.

        Процедура Production OAuth verification.

        Сложная обработка ошибок и кэширование.

        Логика повторных попыток (retry logic).

    Связность авторизации: Метод авторизации встроен напрямую в сервис работы с Google API, а не вынесен в универсальный модуль.

    Проверка слотов: После выбора слота пользователем система не проверяет повторно, не стал ли он занят непосредственно перед бронированием (возможен race condition).

AI и RAG (Retrieval-Augmented Generation)

    Облачные эмбеддинги: Используются готовые облачные решения; локальные модели для генерации векторов не применяются.

    Упрощенный RAG: Логика сервиса поиска и выдачи контекста реализована в базовом варианте.

    Синхронизация при смене модели: При замене модели в RAGService изменения не подтягиваются в базу данных автоматически (требуется переиндексация).

    Оптимизация токенов: Промпты не оптимизированы для минимального расхода токенов.

📌 Основные допущения (Assumptions)

    Длительность слота: Для логики бронирования принята константа — 30 минут.

    Поведение API: Предполагается, что текущие юнит- и интеграционные тесты корректно имитируют реальное поведение Google API, включая граничные случаи и пересечения событий.

    Окружение: Система рассчитана на запуск в linux
